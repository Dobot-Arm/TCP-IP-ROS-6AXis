<launch>

  <!-- This file makes it easy to include the settings for trajectory execution  -->
  <arg name="fake_execution_type" default="interpolate" />

  <!-- Flag indicating whether MoveIt! is allowed to load/unload  or switch controllers -->
  <arg name="moveit_manage_controllers" default="true"/>
  <param name="moveit_manage_controllers" value="$(arg moveit_manage_controllers)"/>

  <param name="trajectory_execution/allowed_goal_duration_margin" value="0.5"/>
  <param name="trajectory_execution/allowed_start_tolerance" value="0.1"/>

  <!-- Load the robot specific controller manager; this sets the moveit_controller_manager ROS parameter -->
  <arg name="moveit_controller_manager" default="me6_robot" />
  <include file="$(find me6_moveit)/launch/$(arg moveit_controller_manager)_moveit_controller_manager.launch.xml">
    <arg name="fake_execution_type" value="$(arg fake_execution_type)" />
  </include>

</launch>
